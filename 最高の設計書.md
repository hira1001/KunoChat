# Whisp 最高設計書

> PC右上に常駐する、2人専用の超シンプル送受信チャット。  
> 書けばテキスト。貼ればスクショ。落とせばファイル。届いたらOS通知。  
> Windows / macOS の両方で、OSの一部のように振る舞う。

- 作成日: 2026-06-10
- 対象OS: Windows 10/11, macOS 13+
- 想定プロダクト名: Whisp / Kuno Drop
- 設計方針: 絶対無料・クラウド保存なし・2人専用・白基調・OSネイティブ・右上常駐
- ドキュメント種別: プロダクト設計 / UIUX設計 / 技術設計 / OSネイティブ設計 / セキュリティ設計 / 実装計画

---

## 0. この設計書の目的

この設計書は、PCの右上に表示される小さなチャット兼ファイル送信アプリを、実際に開発できる粒度まで落とし込むためのもの。

単なるアイデア集ではなく、以下をすべて含む。

- プロダクト思想
- 無料構成の制約と回避策
- Windows / macOS 両対応のOSネイティブ仕様
- UI/UX仕様
- 送れるものの設計
- 通信設計
- WebRTC転送プロトコル
- ペアリング設計
- ローカル保存設計
- セキュリティ設計
- エラー設計
- 実装ディレクトリ構成
- フェーズ別開発計画
- 受け入れ基準

---

## 1. 最上位コンセプト

### 1.1 一言でいうと

**PC右上にある、2人専用の送信ポケット。**

チャットアプリというより、次の3つの中間にある。

```text
LINEの簡易チャット
+ AirDropの気軽さ
+ クリップボード送信の速さ
```

### 1.2 体験の理想

ユーザーはアプリを「使っている」と感じない。  
必要な時にだけ、右上に小さな白い箱がある。

```text
書く    → テキストが届く
貼る    → スクショや画像が届く
落とす  → ファイルが届く
開く    → Finder / Explorerで開ける
閉じる  → 常駐して邪魔しない
```

### 1.3 プロダクトの人格

- 静か
- 速い
- 軽い
- 余白がある
- 説明しすぎない
- でも失敗時は正直
- Webアプリっぽくない
- OSの一部のように振る舞う

---

## 2. 絶対に守る要件

### 2.1 Must

| 要件 | 内容 |
|---|---|
| 完全無料 | 有料クラウド、有料TURN、有料DBを使わない |
| Windows / Mac両対応 | Windows 10/11 と macOS を必須対象にする |
| 右上常駐 | PC画面の右上に小さく表示 |
| チャットのみ | SNS化、タイムライン化、グループ化しない |
| 送信対象は幅広く | テキスト、画像、スクショ、ファイル、複数ファイル |
| シンプルUI | ボタンを増やさず、入力欄と＋だけで完結 |
| OSネイティブ | 通知、トレイ/メニューバー、ファイル操作、ショートカット |
| クラウド保存なし | ファイル本体も履歴もクラウド保存しない |
| ログインなし | メール、電話番号、OAuthログインを不要にする |
| 2人専用 | 友達リスト、グループ、公開プロフィールなし |

### 2.2 Must Not

| 禁止 | 理由 |
|---|---|
| ファイルをクラウドに保存する | 容量・転送量・課金リスクが発生 |
| 有料TURNサーバーに依存する | 絶対無料要件に反する |
| 多機能チャット化する | UIが重くなり、思想がぶれる |
| ログイン必須にする | 導入の摩擦が増える |
| 常時大きな画面を出す | 邪魔になる |
| 既読、スタンプ、通話を最初から入れる | MVPの完成度が下がる |
| 受信ファイルを自動実行する | セキュリティリスク |
| OSらしくない独自挙動を優先する | ネイティブ感が損なわれる |

---

## 3. 採用技術スタック

### 3.1 推奨スタック

```text
Desktop:
- Tauri v2

Frontend:
- React
- TypeScript
- Vite

UI:
- Tailwind CSS
- Radix UI primitives
- Lucide Icons
- Framer Motion または CSS transition中心

Native:
- Tauri plugins
  - fs
  - dialog
  - opener
  - notification
  - global-shortcut
  - clipboard
  - autostart
  - single-instance
  - window-state
  - store
  - sql

Realtime signaling:
- Pusher Channels Sandbox

Transfer:
- WebRTC DataChannel

Local storage:
- SQLite
- App config: Tauri Store
- Received files: Downloads/Whisp
```

### 3.2 なぜTauriか

このアプリは、ただのWeb画面ではなく、OSに深く馴染む必要がある。

Tauriを採用する理由:

- Windows / macOS を同一コードベースで狙える
- ReactなどのWeb UIを使える
- Rust側でOS機能にアクセスできる
- Electronより軽量な方向に寄せやすい
- システムトレイ、通知、ファイル操作、ショートカットなどを扱える
- ネイティブアプリとして配布できる

### 3.3 ElectronではなくTauriを優先する理由

| 観点 | Tauri | Electron |
|---|---|---|
| 軽量性 | 強い | Chromium同梱で重くなりがち |
| OS統合 | Rust側で強い | Node連携で可能 |
| Web技術 | 使える | 使える |
| 配布サイズ | 小さめにしやすい | 大きくなりやすい |
| セキュリティ思想 | 権限スコープが明確 | 設計次第 |
| 今回の用途 | かなり合う | 実現可能だが重め |

Electronでも実現はできる。  
ただし、右上常駐・軽い・ネイティブ・邪魔しない、という思想にはTauriがより合う。

---

## 4. 無料構成の現実設計

### 4.1 「絶対無料」の定義

このプロダクトにおける「絶対無料」は以下の意味にする。

```text
- ユーザーに課金しない
- 運営者にも固定費を発生させない
- ファイル保存にクラウド課金を使わない
- 有料中継サーバーを使わない
- 無料枠の範囲で成立する設計にする
```

ただし、外部無料サービスは将来条件が変わる可能性がある。  
そのため、**Pusherに依存しすぎない設計**にする。

### 4.2 通信の役割分担

```text
Pusher:
- ペアリング
- WebRTC offer / answer
- ICE candidate交換
- online / offline状態
- 本文データは送らない

WebRTC DataChannel:
- テキスト
- 画像
- ファイル
- サムネイル
- 進捗
- 受信確認

Local:
- 履歴
- 受信ファイル
- 設定
- ペア情報
```

### 4.3 クラウドに置くもの / 置かないもの

| データ | クラウドに置くか | 理由 |
|---|---:|---|
| roomId | 一時的に置く | 接続に必要 |
| offer / answer | 一時的に置く | WebRTC接続に必要 |
| ICE candidate | 一時的に置く | NAT越えに必要 |
| オンライン状態 | 一時的に置く | UXに必要 |
| テキスト本文 | 置かない | プライバシーと無料維持 |
| 画像 | 置かない | 容量課金リスク |
| ファイル | 置かない | 容量課金リスク |
| 履歴 | 置かない | クラウドDB不要 |
| ユーザープロフィール | 置かない | ログイン不要にする |

### 4.4 有料TURNを使わない方針

WebRTCは通常、STUN/TURNを使う。  
STUNで直接接続できる環境では無料でP2P通信できる。  
しかし、会社・学校・一部モバイル回線・厳しいNAT環境では、TURN中継がないと接続できない場合がある。

このアプリでは、無料を守るために有料TURNを使わない。

そのかわり、失敗時に正直に表示する。

```text
直接接続できませんでした。
同じWi-Fi、または別のネットワークで試してください。
```

### 4.5 フォールバック戦略

完全無料の範囲で、3つの接続経路を持つ。

#### Path A: Remote P2P

```text
Pusherでシグナリング
↓
WebRTC P2P接続
↓
DataChannelで送受信
```

通常の推奨経路。

#### Path B: LAN Pairing

同じWi-Fi内なら、LAN discoveryを使う。

```text
UDP broadcast / mDNS相当
↓
相手候補を発見
↓
ローカルでWebRTCまたはTCP転送
```

注意:
- Windowsではファイアウォール許可が出る可能性がある
- macOSではローカルネットワーク許可が絡む場合がある
- v0.1では必須ではないが、v0.2で入れる価値が高い

#### Path C: Manual Pairing

外部シグナリングが使えない場合、接続文字列をコピーして送る。

```text
AがOffer codeを生成
↓
Bへ別手段で送る
↓
BがAnswer codeを生成
↓
Aへ返す
↓
接続
```

UXは少し重いが、完全無料・外部依存なしの最後の砦になる。

---

## 5. 全体アーキテクチャ

### 5.1 システム全体図

```text
┌───────────────────────────────┐
│ PC A: Tauri App                │
│                               │
│ React UI                       │
│ Chat Store                     │
│ Transfer Manager               │
│ WebRTC PeerConnection          │
│ Local SQLite / File Storage    │
└──────────────┬────────────────┘
               │
               │ WebRTC DataChannel
               │ text / image / file
               │
┌──────────────▼────────────────┐
│ PC B: Tauri App                │
│                               │
│ React UI                       │
│ Chat Store                     │
│ Transfer Manager               │
│ WebRTC PeerConnection          │
│ Local SQLite / File Storage    │
└───────────────────────────────┘


補助:
┌───────────────────────────────┐
│ Pusher Channels Sandbox        │
│ - signaling only               │
│ - no message body              │
│ - no file body                 │
└───────────────────────────────┘
```

### 5.2 アプリ内部レイヤー

```text
UI Layer
- WindowShell
- MiniPill
- MainChat
- Composer
- MessageList
- FileCard

UX Controller Layer
- window state
- drag/drop state
- notification policy
- shortcut handling

Domain Layer
- chat model
- sendable item model
- transfer queue
- pairing state

Transport Layer
- signal client
- WebRTC peer manager
- data channel manager
- transfer protocol

Native Layer
- filesystem
- dialog
- opener
- notification
- tray/menu bar
- global shortcut
- autostart
- window positioning

Persistence Layer
- SQLite
- local files
- settings store
```

### 5.3 重要な設計分離

| 分離 | 理由 |
|---|---|
| Signal と Transferを分ける | Pusherに本文を載せないため |
| Control channel と Binary channelを分ける | 大容量転送中もテキストを遅らせないため |
| Metadata と File bodyを分ける | 履歴管理を軽くするため |
| UI state と Transfer stateを分ける | 転送失敗時もUIを壊さないため |
| OS native command と React UIを分ける | Windows/Mac差分を吸収するため |

---

## 6. Windows / macOS 両対応方針

### 6.1 共通UX

| 機能 | 共通仕様 |
|---|---|
| 右上表示 | メインディスプレイ右上に表示 |
| ミニ状態 | 幅188px、高さ44px |
| 展開状態 | 幅360px、高さ560px |
| 常に前面 | 設定でON/OFF |
| 閉じる | アプリ終了ではなく非表示 |
| 完全終了 | トレイ/メニューのQuit |
| 受信保存 | Downloads/Whisp |
| ファイルを開く | OS既定アプリで開く |
| 保存場所を開く | Finder / Explorerで表示 |
| 通知 | OS通知 |
| ショートカット | 表示/非表示、貼り付け送信、ファイル送信 |
| 自動起動 | 任意設定 |

### 6.2 Windows固有仕様

| 項目 | Windows仕様 |
|---|---|
| 対象 | Windows 10 / 11 |
| 常駐 | システムトレイ |
| 表示位置 | タスクバー位置を考慮して右上 |
| 閉じる | トレイに格納 |
| 通知 | Windows Toast Notification |
| ファイル表示 | Show in Explorer |
| ショートカット | Ctrl + Shift + Space |
| インストーラー | .msi または NSIS .exe |
| タスクバー | 展開中だけ表示、ミニ時は非表示でもよい |
| 転送進捗 | タスクバー進捗表示を検討 |
| フォント | Segoe UI / system-ui |
| 影 | macOSより少し境界を強く |
| 注意 | Windows Defender / Firewallの警告に配慮 |

### 6.3 macOS固有仕様

| 項目 | macOS仕様 |
|---|---|
| 対象 | macOS 13+ |
| 常駐 | メニューバーアイコン |
| 表示位置 | メニューバー下の右上 |
| 閉じる | ウィンドウ非表示 |
| 通知 | macOS通知 |
| ファイル表示 | Reveal in Finder |
| ショートカット | Cmd + Shift + Space |
| インストーラー | .dmg |
| Dock表示 | 設定でON/OFF。初期はDock非表示寄り |
| フォント | SF Pro / system-ui |
| 影 | 軽く、自然に |
| 注意 | Gatekeeper、署名、ノータリゼーションを考慮 |

### 6.4 OS差分の抽象化

```ts
type PlatformAdapter = {
  platform: "windows" | "macos";

  showMainWindow(): Promise<void>;
  hideMainWindow(): Promise<void>;
  positionTopRight(): Promise<void>;

  openFile(path: string): Promise<void>;
  revealFile(path: string): Promise<void>;
  openDownloads(): Promise<void>;

  showNotification(input: NativeNotification): Promise<void>;

  registerShortcuts(): Promise<void>;
  setAutostart(enabled: boolean): Promise<void>;

  setTaskProgress?(progress: number | null): Promise<void>;
};
```

UIは `PlatformAdapter` を呼ぶだけ。  
OS固有処理はRust/Tauriコマンド側に閉じ込める。

---

## 7. OSネイティブ機能設計

### 7.1 必須ネイティブ機能一覧

| 機能 | 必須度 | 目的 |
|---|---:|---|
| システムトレイ / メニューバー | Must | 常駐とメニュー |
| グローバルショートカット | Must | いつでも呼び出せる |
| ネイティブ通知 | Must | 新着と受信完了 |
| ネイティブファイルダイアログ | Must | ファイル選択 |
| OSクリップボード | Must | スクショ/画像/テキスト貼り付け |
| Drag & Drop | Must | ファイルを落として送信 |
| OSで開く | Must | Open |
| Finder / Explorer表示 | Must | Reveal |
| 自動起動 | Should | 常駐体験 |
| 単一起動 | Must | 二重起動防止 |
| ウィンドウ状態保存 | Should | 位置復元 |
| タスクバー進捗 | Should | Windowsで大容量送信時 |
| Dock非表示 | Could | macOSメニューバーアプリ感 |
| コンテキストメニュー | Should | ファイル操作 |
| ファイル関連付け | Won't v0.1 | 送信先として右クリック登録は将来 |

### 7.2 トレイ / メニューバー

#### Windows

```text
Whisp
────────────
Open Whisp
Send File...
Send Clipboard
────────────
Connected to Kuno
Mute for 1 hour
Open Downloads
Settings
────────────
Quit
```

#### macOS

メニューバーアイコンから同様のメニューを出す。

```text
Whisp
────────────
Open
Send File...
Send Clipboard
────────────
Connected to Kuno
Mute for 1 hour
Open Downloads
Settings...
────────────
Quit Whisp
```

### 7.3 ショートカット

| 操作 | Windows | macOS |
|---|---|---|
| 表示/非表示 | Ctrl + Shift + Space | Cmd + Shift + Space |
| クリップボード送信 | Ctrl + Shift + V | Cmd + Shift + V |
| ファイル選択 | Ctrl + O | Cmd + O |
| 受信フォルダを開く | Ctrl + Shift + O | Cmd + Shift + O |
| 設定 | Ctrl + , | Cmd + , |
| 閉じる/隠す | Esc | Esc |

ショートカットは変更可能にするが、v0.1では固定でもよい。

### 7.4 通知ルール

通知は出しすぎない。

| 状態 | 通知 |
|---|---|
| ウィンドウが開いていてフォーカス中 | 出さない |
| ミニ状態 | 未読ドットのみ |
| 非表示状態 | OS通知 |
| ファイル受信完了 | OS通知 |
| 転送失敗 | OS通知 + ミニ状態に赤ドット |
| 連続メッセージ | 1通知にまとめる |
| サイレントモード | OS通知を出さず未読だけ |

通知文言:

```text
Kuno sent a file
proposal.pdf · 2.4 MB
```

```text
Kuno
見えてる？
```

### 7.5 ファイルOpen / Reveal

ファイルカードに2つの行動を用意する。

| 操作 | 動作 |
|---|---|
| Click / Double click | OS既定アプリで開く |
| Alt/Option + click | Finder / Explorerで表示 |
| 右クリック | Open / Reveal / Copy / Save as / Delete local copy |

---

## 8. 画面構成

### 8.1 画面は3つだけ

| 画面 | 用途 |
|---|---|
| Mini Pill | 普段の常駐状態 |
| Main Chat | 送受信画面 |
| Pairing | 初回接続画面 |

設定はMain Chatからモーダルとして開く。  
独立した複雑な設定画面にはしない。

### 8.2 Mini Pill

```text
┌──────────────────────┐
│  Whisp          ●    │
└──────────────────────┘
```

仕様:

```text
width: 188px
height: 44px
radius: 22px
background: #FFFFFF
border: 1px solid #EDEDED
shadow: soft
position: top-right
alwaysOnTop: true by default
```

状態表示:

| 表示 | 意味 |
|---|---|
| 緑点 | connected |
| 黒点 | unread |
| グレー点 | offline |
| 赤点 | failed |
| 細い線 | transferring |

操作:

| 操作 | 挙動 |
|---|---|
| クリック | 展開 |
| ダブルクリック | Main Chat表示 |
| ファイルドロップ | 添付候補に入れて展開 |
| 右クリック | OSネイティブメニュー |
| ショートカット | 表示/非表示 |

### 8.3 Main Chat

```text
┌────────────────────────────────┐
│  Whisp              ● Online  × │
├────────────────────────────────┤
│                                │
│  Today                         │
│                                │
│  ┌──────────────────────┐      │
│  │ screenshot.png        │      │
│  │ 842 KB         Open   │      │
│  └──────────────────────┘      │
│                                │
│              ┌──────────────┐  │
│              │ 見た！        │  │
│              └──────────────┘  │
│                    22:41       │
│                                │
├────────────────────────────────┤
│  ＋  Message...             ↑  │
└────────────────────────────────┘
```

仕様:

```text
width: 360px
height: 560px
radius: 22px
header: 52px
composer: 64px
message padding: 12px
message gap: 10px
```

### 8.4 Pairing

```text
┌────────────────────────────────┐
│  Whisp                         │
│                                │
│  Pair with your friend         │
│                                │
│  Your code                     │
│  ┌──────────────┐              │
│  │  8K2Q-19      │              │
│  └──────────────┘              │
│                                │
│  Friend code                   │
│  ┌──────────────┐              │
│  │              │              │
│  └──────────────┘              │
│                                │
│  Connect                       │
└────────────────────────────────┘
```

### 8.5 Settings

```text
┌────────────────────────────────┐
│  Settings                    ×  │
├────────────────────────────────┤
│  Name                          │
│  [ Atsushi ]                   │
│                                │
│  Save files to                 │
│  Downloads/Whisp        Change │
│                                │
│  Always on top           ON    │
│  Launch at login         ON    │
│  Sound                   ON    │
│  Notifications           ON    │
│                                │
│  Shortcut                      │
│  Ctrl + Shift + Space          │
│                                │
│  Clear history                 │
└────────────────────────────────┘
```

---

## 9. UIビジュアル設計

### 9.1 デザイン方向

- 白基調
- 黒と薄グレー
- 角丸
- 余白多め
- 罫線は極薄
- 影は軽く
- 色は状態表示にだけ使う
- ボタンを増やさない
- デザインの主張より操作の気持ちよさを優先

### 9.2 Design Tokens

```css
:root {
  --bg: #FFFFFF;
  --surface: #FAFAFA;
  --surface-hover: #F5F5F5;
  --surface-active: #EFEFEF;

  --border: #EDEDED;
  --border-strong: #D4D4D4;

  --text: #111111;
  --muted: #737373;
  --faint: #A3A3A3;

  --me: #111111;
  --me-text: #FFFFFF;
  --other: #F4F4F5;
  --other-text: #111111;

  --success: #16A34A;
  --danger: #DC2626;
  --warning: #D97706;

  --radius-window: 22px;
  --radius-card: 16px;
  --radius-input: 18px;
  --radius-pill: 999px;

  --shadow-window:
    0 18px 50px rgba(0, 0, 0, 0.10),
    0 2px 8px rgba(0, 0, 0, 0.06);

  --shadow-card:
    0 1px 2px rgba(0, 0, 0, 0.04);
}
```

### 9.3 フォント

| OS | UIフォント | コード |
|---|---|---|
| macOS | SF Pro / system-ui | SF Mono / ui-monospace |
| Windows | Segoe UI / system-ui | Cascadia Mono / ui-monospace |
| 日本語 | system-ui / Noto Sans JP fallback | ui-monospace |

### 9.4 アイコン方針

アイコンは最小限。

| 場所 | アイコン |
|---|---|
| ＋ | 添付 |
| ↑ | 送信 |
| × | 閉じる / 添付削除 |
| ● | 状態 |
| ファイル種別 | テキストラベルで代用 |

ファイルアイコンにカラフルな画像を多用しない。  
`PDF`, `IMG`, `ZIP`, `DOC` のような小さなラベルで十分。

---

## 10. UXインタラクション設計

### 10.1 基本操作

ユーザーが覚えることは4つだけ。

```text
書く
貼る
落とす
選ぶ
```

### 10.2 Composer

```text
┌────────────────────────────────┐
│  ＋  Message...             ↑  │
└────────────────────────────────┘
```

| 操作 | 挙動 |
|---|---|
| Enter | 送信 |
| Shift + Enter | 改行 |
| ＋ | OSファイル選択 |
| Drag & Drop | 送信候補に追加 |
| Paste | 内容に応じて追加 |
| Esc | 添付解除、なければ隠す |
| Cmd/Ctrl + L | 入力欄フォーカス |

### 10.3 Drag & Drop

ドラッグ中:

```text
┌────────────────────────────────┐
│                                │
│          Drop to send           │
│                                │
└────────────────────────────────┘
```

ドロップ後:

```text
┌────────────────────────────────┐
│  screenshot.png        ×        │
│  proposal.pdf          ×        │
├────────────────────────────────┤
│  Message...                 ↑  │
└────────────────────────────────┘
```

誤送信防止のため、ドロップしただけでは即送信しない。  
ただし、設定で「Drop to send immediately」を将来追加してもよい。

### 10.4 Clipboard UX

| 内容 | 挙動 |
|---|---|
| テキスト | 入力欄に入る |
| URL | リンク扱い |
| 画像 | 添付プレビュー |
| スクショ | `Screenshot HH-mm.png` として添付 |
| ファイル | 添付プレビュー |
| 複数ファイル | Bundle化 |

スクショ貼り付け例:

```text
┌────────────────────────────────┐
│  Screenshot 22-41.png    ×      │
├────────────────────────────────┤
│  ここ見て                 ↑     │
└────────────────────────────────┘
```

### 10.5 マイクロインタラクション

| 対象 | 動き |
|---|---|
| ミニ→展開 | 160ms, ease-out |
| 送信カード | 下から8px浮いて入る |
| 受信カード | 少しだけfade in |
| ドロップ中 | 背景が薄く変化 |
| 送信完了 | 進捗バーが自然に消える |
| 新着 | ミニ状態が1回だけpulse |
| 失敗 | 赤を大きく出さず、点と小文字で示す |

アニメーションは「気持ちいいが邪魔しない」が基準。

---

## 11. 送れるものの設計

### 11.1 送信対象カテゴリ

| 種類 | v0.1 | 表示 | 備考 |
|---|---:|---|---|
| テキスト | Must | 吹き出し | 最大20,000文字 |
| URL | Must | テキスト/リンクカード | OGP取得なし |
| 画像 | Must | プレビューカード | png/jpg/webp/gif |
| スクショ | Must | 画像カード | Clipboardから |
| PDF | Must | ファイルカード | Open |
| ZIP | Must | ファイルカード | Open |
| Office | Must | ファイルカード | docx/xlsx/pptx |
| 動画 | Should | ファイルカード | mp4/mov/webm |
| 音声 | Should | ファイルカード | mp3/wav/m4a |
| コードファイル | Should | ファイルカード | js/ts/py等 |
| 複数ファイル | Must | Bundleカード | まとめる |
| フォルダ | Could | zip化 | v0.2推奨 |
| 任意バイナリ | Must | FILEカード | 種別不明も送る |

### 11.2 SendableItem

すべてを同じ抽象で扱う。

```ts
type SendableKind =
  | "text"
  | "link"
  | "image"
  | "file"
  | "bundle"
  | "folder"
  | "code";

type SendableItem = {
  id: string;
  kind: SendableKind;

  senderId: string;
  senderName: string;
  createdAt: number;

  status:
    | "draft"
    | "queued"
    | "preparing"
    | "sending"
    | "sent"
    | "receiving"
    | "received"
    | "saved"
    | "failed"
    | "cancelled";

  progress?: number;
  content?: TextContent | LinkContent | CodeContent;
  asset?: AssetContent;
  bundle?: BundleContent;

  error?: {
    code: string;
    message: string;
  };
};
```

### 11.3 Text

```ts
type TextContent = {
  text: string;
  plainText: string;
  length: number;
};
```

仕様:

| 項目 | 内容 |
|---|---|
| 最大文字数 | 20,000 |
| 改行 | Shift+Enter |
| URL | 自動リンク |
| 長文 | 折りたたみ |
| コード | monospace表示に切り替え可能 |

### 11.4 Link

```ts
type LinkContent = {
  url: string;
  host: string;
  title?: string;
};
```

v0.1ではOGP取得しない。  
外部HTTP取得は遅延、失敗、プライバシー、複雑性を増やすため。

表示:

```text
┌──────────────────────┐
│  example.com          │
│  https://example.com  │
└──────────────────────┘
```

### 11.5 Image

```ts
type ImageAsset = {
  kind: "image";
  name: string;
  size: number;
  mime: string;
  width?: number;
  height?: number;
  thumbnailPath?: string;
  localPath?: string;
  transferId: string;
  sha256?: string;
};
```

対応:

```text
png
jpg
jpeg
webp
gif
bmp
svg
```

HEICはmacOSでは扱いやすいがWindowsで不安定になりやすいためv0.2以降。

画像カード:

```text
┌──────────────────────┐
│  [ image preview ]    │
│  screenshot.png       │
│  842 KB        Open   │
└──────────────────────┘
```

### 11.6 File

```ts
type FileAsset = {
  kind: "file";
  name: string;
  extension: string;
  size: number;
  mime: string;
  localPath?: string;
  savePath?: string;
  transferId: string;
  sha256?: string;
};
```

表示分類:

| 種類 | 拡張子 | ラベル |
|---|---|---|
| PDF | pdf | PDF |
| 文書 | doc/docx/pages | DOC |
| 表計算 | xls/xlsx/csv | XLS |
| プレゼン | ppt/pptx/key | PPT |
| 圧縮 | zip/rar/7z | ZIP |
| 動画 | mp4/mov/webm | VID |
| 音声 | mp3/wav/m4a | AUD |
| 画像 | png/jpg/webp | IMG |
| コード | js/ts/py/html/css | CODE |
| 不明 | その他 | FILE |

### 11.7 Bundle

複数ファイルや、テキスト+ファイルをまとめる。

```ts
type BundleContent = {
  caption?: string;
  items: AssetContent[];
  totalSize: number;
  count: number;
  transferId: string;
};
```

表示:

```text
┌──────────────────────┐
│  4 files              │
│  screenshot.png        │
│  proposal.pdf          │
│  +2 more               │
│  12.8 MB       Open    │
└──────────────────────┘
```

### 11.8 Mixed Bundle

例:

```text
「これ確認して」
+ proposal.pdf
+ screenshot.png
```

これは1つのカードにする。

```text
┌──────────────────────┐
│  これ確認して          │
│                        │
│  proposal.pdf          │
│  screenshot.png        │
│  3.1 MB         Send   │
└──────────────────────┘
```

### 11.9 送信制限

v0.1:

```text
1ファイル最大: 500 MB
同時送信: 3件
キュー最大: 20件
テキスト最大: 20,000文字
画像サムネイル: 最大512px
チャンクサイズ: 128KB
```

v0.2:

```text
1ファイル最大: 2GB
一時停止 / 再開
フォルダ自動zip化
失敗チャンク再送
```

---

## 12. メッセージカード設計

### 12.1 Text Card

自分:

```text
              ┌──────────────┐
              │ 了解、送るね │
              └──────────────┘
                    22:41
```

相手:

```text
┌──────────────┐
│ 見えてる？   │
└──────────────┘
22:41
```

### 12.2 Image Card

```text
┌──────────────────────┐
│  [ image preview ]    │
│  screenshot.png       │
│  842 KB        Open   │
└──────────────────────┘
```

状態:
- 送信中: progress
- 受信中: blurred placeholder
- 完了: preview
- 失敗: Retry

### 12.3 File Card

```text
┌──────────────────────┐
│  PDF                  │
│  proposal.pdf         │
│  2.4 MB        Open   │
└──────────────────────┘
```

危険ファイル:

```text
┌──────────────────────┐
│  EXE                  │
│  installer.exe        │
│  Be careful     Open  │
└──────────────────────┘
```

### 12.4 Transfer State

Queued:

```text
┌──────────────────────┐
│  proposal.pdf         │
│  Waiting...           │
└──────────────────────┘
```

Sending:

```text
┌──────────────────────┐
│  proposal.pdf         │
│  48%  █████░░░░░      │
└──────────────────────┘
```

Failed:

```text
┌──────────────────────┐
│  proposal.pdf         │
│  Failed        Retry  │
└──────────────────────┘
```

---

## 13. ペアリング設計

### 13.1 初回ペアリング

手順:

```text
AがWhispを起動
↓
Your codeが表示される
↓
Bがコードを入力
↓
Pusher上のroomに参加
↓
Offer / Answer交換
↓
DataChannel確立
↓
ペア情報をローカル保存
```

### 13.2 ペアリングコード

```text
表示例: 8K2Q-19
```

内部的には短い表示コードとは別に、十分長いランダムIDを持つ。

```ts
type PairingSession = {
  pairingCode: string;
  roomId: string;       // 128bit+ random
  expiresAt: number;    // 10 minutes
  createdBy: string;
};
```

### 13.3 再接続

一度ペアリングしたら、次回は自動再接続。

保存:

```ts
type PairedPeer = {
  peerId: string;
  displayName: string;
  roomId: string;
  fingerprint?: string;
  pairedAt: number;
  lastSeenAt: number;
};
```

### 13.4 ペアリング失敗

表示:

```text
Couldn’t pair.
Check the code and try again.
```

日本語:

```text
接続できませんでした。
コードを確認してもう一度試してください。
```

---

## 14. 通信設計

### 14.1 Signal Layer

Pusherでは以下だけ扱う。

```ts
type SignalMessage =
  | SignalHello
  | SignalOffer
  | SignalAnswer
  | SignalIceCandidate
  | SignalPresence
  | SignalLeave;
```

本文やファイル情報は送らない。  
ただし `peerId` や `roomId` は送る。

### 14.2 WebRTC Layer

DataChannelを2本作る。

```text
control channel:
- text
- metadata
- transfer init
- progress
- cancel
- retry
- ping/pong

binary channel:
- file chunks
- image chunks
```

### 14.3 なぜ2本に分けるか

大きいファイルをbinary channelで送っている間でも、control channelでテキストやキャンセルを通したいから。

```text
control = すぐ届くべき軽いイベント
binary  = 大きいデータの本体
```

---

## 15. 転送プロトコル設計

### 15.1 Envelope

```ts
type Envelope<T> = {
  v: 1;
  id: string;
  type: string;
  roomId: string;
  senderId: string;
  createdAt: number;
  payload: T;
};
```

### 15.2 Text送信

```json
{
  "v": 1,
  "id": "msg_01",
  "type": "text:create",
  "roomId": "room_...",
  "senderId": "peer_a",
  "createdAt": 1781095200000,
  "payload": {
    "text": "見えてる？"
  }
}
```

### 15.3 Asset Init

```json
{
  "type": "asset:init",
  "payload": {
    "transferId": "tr_123",
    "kind": "file",
    "name": "proposal.pdf",
    "size": 2480000,
    "mime": "application/pdf",
    "totalChunks": 19,
    "chunkSize": 131072,
    "sha256": "..."
  }
}
```

### 15.4 Asset Ready

```json
{
  "type": "asset:ready",
  "payload": {
    "transferId": "tr_123"
  }
}
```

### 15.5 Binary Chunk

binary channelでは、ヘッダー + bodyを送る。

概念:

```text
[magic: 4 bytes]
[headerLength: 4 bytes]
[headerJson: n bytes]
[chunkBytes: remaining]
```

header:

```ts
type ChunkHeader = {
  transferId: string;
  index: number;
  total: number;
  offset: number;
  byteLength: number;
};
```

### 15.6 Progress

```json
{
  "type": "asset:progress",
  "payload": {
    "transferId": "tr_123",
    "receivedBytes": 1048576,
    "totalBytes": 2480000
  }
}
```

### 15.7 Complete

```json
{
  "type": "asset:complete",
  "payload": {
    "transferId": "tr_123",
    "sha256": "..."
  }
}
```

### 15.8 Failed

```json
{
  "type": "asset:failed",
  "payload": {
    "transferId": "tr_123",
    "reason": "hash_mismatch"
  }
}
```

---

## 16. チャンク転送設計

### 16.1 初期値

```text
chunkSize: 128 KB
maxBufferedAmount: 8 MB
lowThreshold: 2 MB
parallelTransfers: 1 large + control messages
```

### 16.2 Backpressure

送信しすぎてDataChannelが詰まらないようにする。

擬似コード:

```ts
async function pump() {
  while (
    channel.readyState === "open" &&
    channel.bufferedAmount < MAX_BUFFERED_AMOUNT &&
    hasMoreChunks()
  ) {
    const chunk = await readNextChunk();
    channel.send(chunk);
    markSent(chunk);
  }

  if (hasMoreChunks()) {
    await waitForBufferedAmountLow();
    pump();
  }
}
```

### 16.3 進捗計算

送信側:

```text
sentBytes / totalBytes
```

受信側:

```text
receivedBytes / totalBytes
```

UIは受信側のprogressを優先する。  
送信済みでも相手が保存できていなければ完了にしない。

### 16.4 ハッシュ検証

送信前にsha256を計算。  
受信後にsha256を再計算。  
一致したらcomplete。

不一致なら:

```text
Transfer failed
Retry
```

### 16.5 再送

v0.1:

```text
失敗したら最初からRetry
```

v0.2:

```text
不足チャンクのみ再送
一時停止/再開
途中再開
```

---

## 17. 送信キュー設計

### 17.1 Queue Item

```ts
type TransferQueueItem = {
  id: string;
  itemId: string;
  assetId: string;

  priority: "high" | "normal" | "low";

  status:
    | "queued"
    | "preparing"
    | "sending"
    | "paused"
    | "complete"
    | "failed";

  progress: number;
  sentBytes: number;
  totalBytes: number;
};
```

### 17.2 優先順位

| 種類 | 優先度 |
|---|---|
| テキスト | high |
| サムネイル | high |
| 小さい画像 | high |
| 通常ファイル | normal |
| 大きい動画 | low |

### 17.3 大容量送信中のテキスト

大容量ファイル送信中でも、テキストはcontrol channelで送る。  
これが体験品質に直結する。

---

## 18. ローカル保存設計

### 18.1 保存先

初期値:

```text
Downloads/Whisp
```

日付ごと:

```text
Downloads/Whisp/
  2026-06-10/
    screenshot.png
    proposal.pdf
```

Bundle:

```text
Downloads/Whisp/
  2026-06-10/
    Bundle 22-41/
      screenshot.png
      proposal.pdf
```

### 18.2 ファイル名重複

```text
proposal.pdf
proposal 2.pdf
proposal 3.pdf
```

### 18.3 SQLite schema

```sql
CREATE TABLE messages (
  id TEXT PRIMARY KEY,
  kind TEXT NOT NULL,
  sender_id TEXT NOT NULL,
  sender_name TEXT,
  created_at INTEGER NOT NULL,
  status TEXT NOT NULL,
  text TEXT,
  payload_json TEXT,
  deleted_at INTEGER
);

CREATE TABLE assets (
  id TEXT PRIMARY KEY,
  message_id TEXT NOT NULL,
  transfer_id TEXT NOT NULL,
  kind TEXT NOT NULL,
  name TEXT NOT NULL,
  extension TEXT,
  mime TEXT,
  size INTEGER NOT NULL,
  sha256 TEXT,
  local_path TEXT,
  thumbnail_path TEXT,
  status TEXT NOT NULL,
  created_at INTEGER NOT NULL,
  FOREIGN KEY(message_id) REFERENCES messages(id)
);

CREATE TABLE peers (
  id TEXT PRIMARY KEY,
  display_name TEXT,
  room_id TEXT,
  fingerprint TEXT,
  paired_at INTEGER,
  last_seen_at INTEGER
);

CREATE TABLE settings (
  key TEXT PRIMARY KEY,
  value_json TEXT NOT NULL
);
```

### 18.4 履歴保持

初期設定:

```text
テキスト履歴: 無期限
ファイルメタ情報: 無期限
ファイル本体: ユーザーが削除するまで
```

将来設定:

```text
30日後に古いファイルを自動削除
履歴だけ残す
すべて削除
```

---

## 19. セキュリティ設計

### 19.1 基本方針

- 知らない相手から受け取らない
- ペアリングした相手だけ許可
- ファイルは自動実行しない
- 受信後も勝手に開かない
- 危険拡張子は控えめに警告
- ハッシュ検証する
- Pusherに本文を送らない
- ローカル権限を最小化する

### 19.2 危険拡張子

```text
.exe
.bat
.cmd
.scr
.ps1
.vbs
.command
.app
.dmg
.pkg
.msi
.sh
.jar
```

表示:

```text
Be careful
```

派手な警告は不要。  
ただし、初回だけ説明してもよい。

### 19.3 権限スコープ

Tauriの権限は必要最小限にする。

```text
filesystem:
- Downloads/Whisp
- appData/Whisp
- 選択されたファイルパスのみ

opener:
- 受信済みファイル
- 受信フォルダ
- URLは必要時のみ

clipboard:
- ユーザー操作時のみ

notification:
- 許可済みの場合のみ

global shortcut:
- 登録したキーのみ
```

### 19.4 IP/ネットワーク情報

WebRTCの性質上、接続のためにネットワーク情報が扱われる。  
ユーザー向けには細かく出さないが、設計上は以下を明記する。

- 接続のためにP2P通信を使う
- ファイルはクラウドを経由しない
- 直接接続できないネットワークでは失敗する
- 有料中継は使わない

---

## 20. エラー設計

### 20.1 接続エラー

| 状態 | 表示 |
|---|---|
| シグナリング失敗 | Couldn’t reach pairing service |
| 直接接続失敗 | Couldn’t connect directly |
| 相手オフライン | Kuno is offline |
| 再接続中 | Reconnecting... |

日本語:

```text
直接接続できませんでした。
同じWi-Fi、または別のネットワークで試してください。
```

### 20.2 ファイルエラー

| 原因 | 表示 |
|---|---|
| サイズ超過 | File is too large |
| 読み込み失敗 | Couldn’t read file |
| 送信中断 | Transfer interrupted |
| ハッシュ不一致 | File verification failed |
| 保存失敗 | Couldn’t save file |
| 相手オフライン | Files need both devices online |

### 20.3 UI方針

- エラーは小さく出す
- 詳細はクリックで表示
- まずRetryを出す
- 技術用語を表に出しすぎない

---

## 21. 状態設計

### 21.1 AppState

```ts
type AppState =
  | "pairing"
  | "connecting"
  | "connected"
  | "reconnecting"
  | "offline"
  | "failed";
```

### 21.2 WindowState

```ts
type WindowMode =
  | "hidden"
  | "mini"
  | "expanded"
  | "settings"
  | "pairing";
```

### 21.3 ConnectionState

```ts
type ConnectionState = {
  signal: "idle" | "connecting" | "connected" | "failed";
  peer: "idle" | "connecting" | "connected" | "failed" | "closed";
  dataChannels: {
    control: "connecting" | "open" | "closed";
    binary: "connecting" | "open" | "closed";
  };
};
```

### 21.4 TransferState

```ts
type TransferState =
  | "queued"
  | "preparing"
  | "waiting_for_peer"
  | "sending"
  | "receiving"
  | "verifying"
  | "complete"
  | "failed"
  | "cancelled";
```

---

## 22. 空状態設計

### 22.1 未接続

```text
Pair with your friend

Your code
8K2Q-19

Enter friend's code
[        ]

Connect
```

### 22.2 接続済み・履歴なし

```text
Drop something here
or just say hi.
```

### 22.3 オフライン

```text
Kuno is offline.
Text will send when connected.
Files need both devices online.
```

ただしv0.1では、オフライン時にテキストもキューに貯めるかどうかを決める必要がある。

推奨:
- テキストはローカルキューに入れる
- 相手がオンラインになったら送る
- ファイルはキューに入れるが「相手がオンラインになるまで待機」と表示

---

## 23. パフォーマンス設計

### 23.1 目標

| 項目 | 目標 |
|---|---|
| 起動 | 2秒以内 |
| ミニ表示切替 | 100ms以内 |
| 展開アニメーション | 160ms |
| テキスト送信表示 | 即時 |
| ファイルドロップ反応 | 100ms以内 |
| 画像サムネ生成 | 1秒以内 |
| UIフリーズ | なし |
| 大容量送信中 | テキスト送信可能 |

### 23.2 大容量ファイル対策

- ファイル全体をメモリに載せない
- chunk単位で読む
- bufferedAmountで送信制御
- ハッシュ計算は非同期
- UIスレッドを塞がない
- 進捗更新は100ms〜250ms間隔に間引く

### 23.3 履歴表示

- メッセージは仮想リスト化
- 画像は遅延読み込み
- 大きい画像はサムネイルを使用
- 古い履歴は必要時にロード

---

## 24. アクセシビリティ設計

### 24.1 キーボード操作

- Tab順序を自然にする
- Enter送信、Shift+Enter改行
- Escで閉じる
- ファイルカードをキーボードでOpenできる
- 設定はキーボードで操作可能

### 24.2 視認性

- 文字サイズは13px未満にしない
- muted textでもコントラストを保つ
- 進捗は色だけに依存しない
- 状態点にはツールチップを持たせる

### 24.3 Reduce Motion

OSの「視差効果を減らす」に従う。  
該当時はアニメーションを最小化。

---

## 25. 実装ディレクトリ構成

```text
src/
  app/
    App.tsx
    bootstrap.ts
    platform.ts

  components/
    WindowShell.tsx
    MiniPill.tsx
    Header.tsx
    MessageList.tsx
    MessageBubble.tsx
    FileCard.tsx
    BundleCard.tsx
    Composer.tsx
    AttachmentPreview.tsx
    DropOverlay.tsx
    SettingsPanel.tsx

  features/
    pairing/
      PairingScreen.tsx
      pairingStore.ts
      pairingTypes.ts

    chat/
      chatStore.ts
      messageTypes.ts
      messageRepository.ts

    sendables/
      detectSendable.ts
      sendableTypes.ts
      clipboardParser.ts
      dropParser.ts

    transfer/
      transferManager.ts
      transferTypes.ts
      transferQueue.ts
      chunker.ts
      hash.ts
      fileSender.ts
      fileReceiver.ts
      thumbnail.ts

    rtc/
      peerConnection.ts
      dataChannels.ts
      rtcEvents.ts
      rtcTypes.ts

    signal/
      pusherClient.ts
      signalTypes.ts
      signalRouter.ts

    storage/
      db.ts
      migrations.ts
      fileStorage.ts
      settingsStore.ts

    native/
      platformAdapter.ts
      shortcuts.ts
      notifications.ts
      tray.ts
      windowPosition.ts
      opener.ts

  styles/
    globals.css
    tokens.css

src-tauri/
  src/
    main.rs
    lib.rs
    commands/
      window.rs
      fs.rs
      opener.rs
      notification.rs
      platform.rs
    native/
      tray.rs
      shortcuts.rs
      autostart.rs
```

---

## 26. 開発フェーズ

### Phase 0: 技術検証

目的:
- Tauriで右上常駐できるか
- Windows/Macでトレイ/メニューバーが動くか
- WebRTC DataChannelがTauri WebViewで問題ないか
- ファイルチャンク送信ができるか

成果:
- 小さなPoC
- 2台間でテキスト送信
- 1MBファイル送信

### Phase 1: UI完成

実装:
- MiniPill
- MainChat
- Composer
- FileCard
- BundleCard
- DropOverlay
- Settings
- design tokens

通信なしでUIだけ完璧にする。

### Phase 2: ローカル送信体験

実装:
- テキストを自分の画面に出す
- ファイルカードを出す
- 画像サムネイル
- ドラッグ&ドロップ
- クリップボード貼り付け
- SQLite保存

### Phase 3: ペアリング

実装:
- Pusher接続
- pairing code
- room join
- offer/answer
- ICE candidate
- reconnect

### Phase 4: WebRTCテキスト

実装:
- control channel
- text:create
- ping/pong
- online/offline
- ローカル履歴

### Phase 5: ファイル転送

実装:
- asset:init
- asset:ready
- binary chunk
- progress
- sha256 verification
- save to Downloads/Whisp
- Open / Reveal

### Phase 6: OSネイティブ仕上げ

実装:
- tray/menu bar
- global shortcut
- notifications
- autostart
- single instance
- window state
- taskbar progress on Windows
- macOS menu bar polish

### Phase 7: 品質改善

実装:
- エラー改善
- 再接続
- ファイル失敗Retry
- 大容量テスト
- UI polish
- Windows/Mac実機検証

---

## 27. MVPスコープ

### 27.1 v0.1に入れる

- Windows / macOS対応
- 右上常駐
- ミニ/展開
- 常に前面
- トレイ/メニューバー
- グローバルショートカット
- ペアリング
- WebRTC接続
- テキスト送受信
- 画像送受信
- ファイル送受信
- 複数ファイルBundle
- Drag & Drop
- Clipboard paste
- Local SQLite履歴
- Downloads/Whisp保存
- Open / Reveal
- OS通知
- 危険ファイル警告
- 失敗Retry

### 27.2 v0.1に入れない

- 既読
- スタンプ
- リアクション
- 友達リスト
- グループ
- 通話
- クラウド同期
- ログイン
- 検索
- 画像編集
- 音声/動画のアプリ内再生
- 自動アップデート
- フォルダ自動zip化
- 途中再開

### 27.3 v0.2候補

- フォルダ送信
- LAN discovery
- 手動ペアリング
- 一時停止/再開
- 不足チャンクのみ再送
- 音声プレビュー
- 動画サムネイル
- 自動アップデート
- 右クリック「Whispで送信」
- 複数ディスプレイ最適化
- 設定の高度化

---

## 28. 配布設計

### 28.1 Windows

形式:
- `.msi`
- `.exe` installer

要件:
- スタートメニュー登録
- トレイ常駐
- 自動起動設定
- アンインストール対応
- 通知が正常に出る形でインストール

### 28.2 macOS

形式:
- `.dmg`

要件:
- Applicationsへドラッグ
- Apple Silicon / Intel対応を検討
- 署名とノータリゼーション
- メニューバー常駐
- Gatekeeperで不安を出さない

### 28.3 アップデート

v0.1では手動更新でもよい。  
v0.2以降で自動アップデートを検討。

ただし、絶対無料方針のため、アップデート配布はGitHub Releasesなど無料範囲を優先。

---

## 29. テスト設計

### 29.1 単体テスト

- sendable detection
- URL判定
- MIME判定
- file extension分類
- filename重複処理
- chunk header encode/decode
- progress計算
- sha256検証
- settings load/save

### 29.2 結合テスト

- Pusher signaling
- WebRTC接続
- text送受信
- 画像送受信
- 100MBファイル送受信
- 失敗時Retry
- 再接続
- オフライン表示

### 29.3 OSテスト

Windows:
- 起動
- トレイ
- 通知
- Explorer表示
- ショートカット
- インストール/アンインストール
- Windows Defender警告
- 複数ディスプレイ

macOS:
- 起動
- メニューバー
- 通知許可
- Finder表示
- ショートカット
- Gatekeeper
- Dock表示/非表示
- 複数ディスプレイ

### 29.4 ネットワークテスト

| ケース | 期待 |
|---|---|
| 同じWi-Fi | 接続成功 |
| 別ネットワーク | STUNで成功する場合は成功 |
| 厳しいNAT | 失敗表示 |
| 途中でWi-Fi切断 | reconnecting |
| 相手終了 | offline |
| 大容量送信中断 | failed + retry |

---

## 30. 受け入れ基準

### 30.1 UX受け入れ基準

- 起動後、右上に自然に表示される
- 画面が邪魔にならない
- 1分以内に初回ペアリングできる
- テキスト送信に迷わない
- ファイル送信に迷わない
- スクショ貼り付けが直感的
- 受信ファイルをすぐ開ける
- 閉じても終了しないことが自然に感じる
- WindowsでもMacでもOSらしく感じる

### 30.2 技術受け入れ基準

- Windows / Macでビルドできる
- 2台間でWebRTC接続できる
- 100MBファイルを送れる
- UIが固まらない
- ファイル本体がクラウドを通らない
- 履歴がローカルに残る
- 受信ファイルがDownloads/Whispに保存される
- 危険拡張子で警告が出る
- 相手オフライン時に明確な表示が出る

---

## 31. プライバシー設計

### 31.1 基本方針

- メッセージ本文をサーバーに保存しない
- ファイル本体をサーバーに保存しない
- ユーザーアカウントを作らない
- 連絡先をアップロードしない
- 送受信履歴はローカルのみ

### 31.2 ローカルデータ

ローカルに保存されるもの:

```text
- 自分の表示名
- ペアリング済み相手情報
- メッセージ履歴
- ファイルメタデータ
- 受信ファイル
- 設定
```

### 31.3 削除

設定に以下を用意する。

```text
Clear chat history
Delete received files
Reset pairing
Reset all data
```

---

## 32. 将来拡張の余地

追加しても思想を壊しにくいもの:

- フォルダ送信
- LAN discovery
- 右クリック送信
- 音声プレビュー
- 動画サムネイル
- 履歴検索
- 自動アップデート
- 送信先を1人だけでなく2〜3人まで拡張

思想を壊しやすいもの:

- グループチャット
- SNSプロフィール
- スタンプマーケット
- 既読プレッシャー
- 広告
- アカウント必須
- クラウド履歴
- AI要約などの過剰機能

---

## 33. 重要なプロダクト判断

### 33.1 画面を増やさない

送れるものが増えても、画面は増やさない。  
操作は増やさない。  
全部「書く・貼る・落とす・選ぶ」に集約する。

### 33.2 失敗を隠さない

P2P接続できない時は、変にごまかさない。  
有料TURNを使わない設計では、失敗する環境がある。  
それをUIで正直に伝える。

### 33.3 ネイティブに寄せる

ファイルを開く、保存場所を開く、通知、ショートカット、常駐は、OS標準の感覚に合わせる。  
独自の変なファイルビューアを作らない。

### 33.4 UIより体験を優先する

美しさは、見た目だけではない。  
反応の速さ、迷わなさ、通知の少なさ、閉じた時の自然さ、ファイルがFinder/Explorerで開けることまで含めて美しさ。

---

## 34. 最終仕様サマリー

```text
Product:
Whisp

Concept:
PC右上にある、2人専用の送信ポケット

Platforms:
Windows 10/11
macOS 13+

Core:
- 右上常駐
- ミニ/展開
- 2人専用
- ログインなし
- クラウド保存なし
- 完全無料構成

Send:
- text
- URL
- image
- screenshot
- file
- multiple files
- bundle

Operation:
- write
- paste
- drop
- choose

Native:
- tray/menu bar
- global shortcut
- notifications
- file dialog
- clipboard
- drag/drop
- open/reveal
- autostart
- single instance
- window state

Transport:
- Pusher signaling only
- WebRTC DataChannel transfer
- control channel + binary channel
- chunked transfer
- sha256 verification
- no paid TURN

Storage:
- SQLite local history
- Downloads/Whisp received files
- Tauri Store settings

Design:
- white
- quiet
- minimal
- rounded
- thin border
- soft shadow
- no clutter
```

---

## 35. 参考情報

設計時点で参照した主要公式資料・技術資料。

- Tauri v2: https://v2.tauri.app/
- Tauri System Tray: https://v2.tauri.app/learn/system-tray/
- Tauri Window Customization: https://v2.tauri.app/learn/window-customization/
- Tauri Config alwaysOnTop: https://v2.tauri.app/reference/config/
- Tauri File System plugin: https://v2.tauri.app/plugin/file-system/
- Tauri SQL plugin: https://v2.tauri.app/plugin/sql/
- Tauri Opener plugin: https://v2.tauri.app/plugin/opener/
- Tauri Clipboard plugin: https://v2.tauri.app/plugin/clipboard/
- Tauri Notification plugin: https://v2.tauri.app/plugin/notification/
- Tauri Global Shortcut plugin: https://v2.tauri.app/plugin/global-shortcut/
- Tauri Autostart plugin: https://v2.tauri.app/plugin/autostart/
- Tauri Single Instance plugin: https://v2.tauri.app/plugin/single-instance/
- Tauri Window State plugin: https://v2.tauri.app/plugin/window-state/
- Tauri Windows Installer: https://v2.tauri.app/distribute/windows-installer/
- Pusher Channels Pricing: https://pusher.com/channels/pricing/
- MDN RTCDataChannel: https://developer.mozilla.org/en-US/docs/Web/API/RTCDataChannel
- MDN Using WebRTC data channels: https://developer.mozilla.org/en-US/docs/Web/API/WebRTC_API/Using_data_channels
- MDN RTCDataChannel bufferedAmount: https://developer.mozilla.org/en-US/docs/Web/API/RTCDataChannel/bufferedAmount
- MDN RTCDataChannel bufferedAmountLowThreshold: https://developer.mozilla.org/en-US/docs/Web/API/RTCDataChannel/bufferedAmountLowThreshold

---

## 36. 最後に

このアプリで本当に作るべきものは、単なるチャットではない。

**2人のPCの間にある、最短の送信導線**である。

だから、画面は小さくていい。  
機能名も少なくていい。  
説明も少なくていい。  

その代わり、次の体験だけは完璧にする。

```text
右上にある。
すぐ開く。
書ける。
貼れる。
落とせる。
届く。
開ける。
閉じても消えない。
邪魔しない。
無料で動く。
WindowsでもMacでも自然。
```

これがWhispの最高設計である。


---

# Appendix A. UIコンポーネント詳細仕様

## A.1 WindowShell

目的:
- OSウィンドウとReact UIの境界になる
- ミニ状態、展開状態、設定状態、ペアリング状態を切り替える
- ドラッグ可能領域を管理する
- 外側クリックやEscで閉じる挙動を統一する

Props:

```ts
type WindowShellProps = {
  mode: WindowMode;
  connectionState: ConnectionState;
  unreadCount: number;
  activeTransferCount: number;
};
```

責務:
- 角丸背景
- 影
- ウィンドウサイズクラス
- data-tauri-drag-region
- focus trap
- animation wrapper

してはいけないこと:
- メッセージデータを直接読む
- 転送プロトコルを触る
- OSコマンドを直接呼びすぎる

## A.2 MiniPill

状態:
- connected
- offline
- unread
- failed
- transferring

表示:
```text
Whisp ●
```

詳細:
- 未読がある場合は黒点
- 接続中は緑点
- 失敗は赤点
- 転送中は下部に1px progress line
- クリックターゲットは全体
- 右クリックはネイティブメニュー

## A.3 Header

表示:
```text
Whisp              ● Online  ×
```

仕様:
- 高さ52px
- 左側はドラッグ可能
- 状態表示は控えめ
- ×は終了ではなく非表示
- 設定は右クリックかショートカットに逃がす

## A.4 MessageList

責務:
- SendableItemの配列を描画
- 日付セパレーターを出す
- 下端追従
- 新着時のスクロール制御
- 仮想化

ルール:
- ユーザーが過去を見ている時は勝手に下へ飛ばさない
- 新着時は「new」pillを出す
- 同じ送信者の連続メッセージは余白を少し詰める

## A.5 Composer

状態:
- empty
- typing
- hasAttachments
- sending
- offline

入力:
- text
- attachments
- pasted image
- dropped files

送信ボタン:
- 入力が空ならdisabled
- ファイルだけでもenabled
- オフライン時はテキストqueue、ファイルはwaiting表記

## A.6 AttachmentPreview

表示:
```text
screenshot.png ×
proposal.pdf   ×
```

ルール:
- 最大3行まで表示
- 4件以上は `+N more`
- 画像は小サムネ可
- 削除は×
- Bundle化されることを暗に示す

---

# Appendix B. ネイティブコマンド設計

## B.1 window commands

```rust
#[tauri::command]
async fn show_main_window(app: AppHandle) -> Result<(), String>;

#[tauri::command]
async fn hide_main_window(app: AppHandle) -> Result<(), String>;

#[tauri::command]
async fn position_top_right(app: AppHandle) -> Result<(), String>;

#[tauri::command]
async fn set_always_on_top(app: AppHandle, enabled: bool) -> Result<(), String>;
```

## B.2 file commands

```rust
#[tauri::command]
async fn ensure_download_dir() -> Result<String, String>;

#[tauri::command]
async fn reveal_path(path: String) -> Result<(), String>;

#[tauri::command]
async fn open_path(path: String) -> Result<(), String>;

#[tauri::command]
async fn unique_save_path(filename: String) -> Result<String, String>;
```

## B.3 notification commands

```rust
#[tauri::command]
async fn notify_message(title: String, body: String) -> Result<(), String>;

#[tauri::command]
async fn notify_file_received(filename: String, size: u64) -> Result<(), String>;
```

## B.4 platform commands

```rust
#[tauri::command]
async fn get_platform_info() -> PlatformInfo;

#[tauri::command]
async fn register_app_shortcuts() -> Result<(), String>;

#[tauri::command]
async fn set_launch_at_login(enabled: bool) -> Result<(), String>;
```

---

# Appendix C. 状態遷移詳細

## C.1 起動

```text
App start
↓
single instance check
↓
load settings
↓
init tray/menu bar
↓
register shortcuts
↓
restore window mode
↓
connect signal
↓
if paired: reconnect
else: show pairing
```

## C.2 テキスト送信

```text
User types
↓
Enter
↓
Create local message as sent-pending
↓
Send over control channel
↓
Peer ACK
↓
Mark sent
↓
Persist SQLite
```

ACKが返らない場合:
```text
sent-pending
↓ timeout
failed
↓ retry
```

## C.3 ファイル送信

```text
Drop file
↓
Create draft attachment
↓
User presses send
↓
Create local asset card
↓
Prepare metadata
↓
Calculate sha256
↓
asset:init
↓
asset:ready
↓
chunk transfer
↓
progress updates
↓
asset:complete
↓
peer verifies
↓
complete
```

## C.4 受信

```text
asset:init received
↓
Create receiving card
↓
Prepare temp file
↓
Receive chunks
↓
Write chunks
↓
Verify sha256
↓
Move temp to Downloads/Whisp
↓
Show Open
↓
Notify if hidden
```

---

# Appendix D. ローカルファイル保存ポリシー

## D.1 temp保存

転送中は一時ディレクトリに保存。

```text
AppData/Whisp/tmp/tr_123.part
```

完了後:

```text
Downloads/Whisp/YYYY-MM-DD/filename.ext
```

## D.2 失敗時

- `.part`は一定期間残す
- v0.1ではRetry時に最初からやり直す
- アプリ起動時に古いtempを掃除する

掃除ルール:

```text
tmp内で24時間以上古い.partは削除
```

## D.3 削除操作

ユーザーが「Delete local copy」を選んだ場合:

- ファイル本体を削除
- message履歴は残す
- カード表示は `File removed locally`

---

# Appendix E. 詳細セキュリティチェックリスト

## E.1 受信前

- peerIdがペア済みか
- roomIdが一致するか
- message vが対応しているか
- asset sizeが制限内か
- filenameに危険なパスが含まれないか
- `../` を含まないか
- absolute pathを信用しないか

## E.2 保存時

- ファイル名をsanitize
- 保存先を許可ディレクトリ内に限定
- 同名ファイルは上書きしない
- tempからfinalへのmoveをatomicに近づける
- ハッシュ一致後にcomplete

## E.3 開く時

- 自動では開かない
- ユーザー操作のみ
- 危険拡張子は警告
- opener scopeを絞る

## E.4 ログ

ログに含めてよい:
- transferId
- size
- status
- error code

ログに含めない:
- メッセージ本文
- ファイル内容
- private key
- raw pairing secret

---

# Appendix F. 日本語UI文言

## F.1 接続

```text
接続中...
接続済み
再接続中...
相手がオフラインです
直接接続できませんでした
同じWi-Fi、または別のネットワークで試してください
```

## F.2 送信

```text
送信中
受信中
保存中
完了
失敗しました
再試行
キャンセル
```

## F.3 ファイル

```text
ファイルを開く
保存場所を開く
名前をコピー
別名で保存
ローカルコピーを削除
```

## F.4 空状態

```text
ここにファイルをドロップ
またはメッセージを送信
```

```text
相手と接続してください
```

```text
ファイル送信には両方のPCがオンラインである必要があります
```

---

# Appendix G. 画面サイズと複数ディスプレイ

## G.1 右上位置計算

基本:

```text
x = monitor.x + monitor.width - window.width - 24
y = monitor.y + safeTop + 24
```

Windows:
- タスクバーが上にある場合は避ける
- スケール125%, 150%を考慮
- display DPIを考慮

macOS:
- メニューバー高さを考慮
- notch搭載Macでも右上が隠れないようにする
- Spaces切り替え時の挙動を確認

## G.2 モニター変更

外部モニターを外した場合:
- 保存済み座標が画面外ならメインディスプレイ右上に戻す

## G.3 ユーザー移動

ユーザーがウィンドウをドラッグした場合:
- その位置を保存
- 次回復元
- ただし画面外なら補正

---

# Appendix H. 無料構成のリスク管理

## H.1 Pusher無料枠変更リスク

Pusherの無料枠は外部サービスのため将来変わる可能性がある。

対策:
- SignalProviderを抽象化
- Pusher以外に差し替え可能にする
- Manual Pairingを設計に入れる
- LAN discoveryをv0.2で入れる

```ts
interface SignalProvider {
  connect(roomId: string): Promise<void>;
  send(message: SignalMessage): Promise<void>;
  onMessage(handler: (message: SignalMessage) => void): void;
  disconnect(): Promise<void>;
}
```

## H.2 TURNなしによる接続失敗

リスク:
- 一部ネットワークでP2Pできない

対策:
- UXで明確表示
- LAN fallback
- Manual Pairing
- 設定で「接続診断」を追加

## H.3 大容量転送による不安定化

対策:
- 500MB制限
- chunked transfer
- backpressure
- progress
- cancel
- retry

---

# Appendix I. 将来の高度設計案

## I.1 LAN高速モード

同一LANではWebRTCではなく、Rust側でTCP転送する選択肢もある。

メリット:
- 高速
- 安定しやすい
- Pusher不要

デメリット:
- Firewall許可
- 実装が増える
- セキュリティ設計が必要

## I.2 右クリック送信

Windows:
- Explorerのコンテキストメニューから送信

macOS:
- Finder Quick Action / Share extension相当

v0.1では入れない。  
入れるとOSごとの差分が大きい。

## I.3 自動アップデート

GitHub Releasesを使う。  
無料構成を維持できるが、署名や配布UXを慎重に設計する。

---

# Appendix J. 開発者向け初期タスク一覧

## J.1 Repository setup

```text
- create Tauri v2 + React + TS + Vite
- add Tailwind
- add ESLint / Prettier
- add Vitest
- add Playwright if needed
- add GitHub Actions skeleton
```

## J.2 Tauri plugins

```text
- fs
- dialog
- opener
- notification
- global-shortcut
- clipboard
- autostart
- single-instance
- window-state
- store
- sql
```

## J.3 First milestone

```text
- frameless window
- right top position
- mini pill
- expand/collapse
- tray/menu bar
- global shortcut
```

## J.4 Second milestone

```text
- composer
- local messages
- drag/drop files
- attachment preview
- image thumbnail
- SQLite save
```

## J.5 Third milestone

```text
- Pusher signaling
- WebRTC data channel
- text transfer
```

## J.6 Fourth milestone

```text
- file init
- chunk transfer
- progress
- save
- open/reveal
```

---

# Appendix K. 品質の定義

このアプリの品質は、機能数では測らない。

品質は以下で測る。

```text
- 起動が速い
- 邪魔しない
- すぐ送れる
- 迷わない
- OSに馴染む
- 失敗時に分かりやすい
- 無料の制約を破らない
- 画面が散らからない
```

悪い品質:
```text
- ボタンが多い
- 設定が多い
- 通知がうるさい
- エラーが専門用語
- ファイル送信中にテキストが遅れる
- 閉じたら終了してしまう
- どこに保存されたか分からない
```

良い品質:
```text
- 右上にある
- ファイルを落としたら分かる
- 送信進捗が見える
- 届いたらすぐ開ける
- 閉じても常駐している
- 相手がオフラインならはっきり分かる
```
